apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: yamar-bootstrap
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: 'https://github.com/letDouglas/yamar.git'
    targetRevision: HEAD
    path: infra/gitops
    directory:
      recurse: true
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
