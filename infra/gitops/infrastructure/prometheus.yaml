apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: infra-prometheus
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  project: default

  sources:
    - repoURL: 'https://prometheus-community.github.io/helm-charts'
      chart: prometheus
      targetRevision: 28.6.0
      helm:
        releaseName: prometheus
        valueFiles:
          - $yamar-repo/infra/resources/values/prometheus-values.yaml

    - repoURL: 'https://github.com/letDouglas/yamar.git'
      targetRevision: main
      ref: yamar-repo

  destination:
    server: https://kubernetes.default.svc
    namespace: observability

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
